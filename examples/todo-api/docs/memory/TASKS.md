# Tasks

## Active

- None.

## Backlog

See [ROADMAP.md](ROADMAP.md) — nothing scheduled.

## Done (recent)

- 2026-08-02: Initial implementation — `createTodoStore`/`createApp`,
  list/create/delete routes, 7 tests covering the happy path, validation
  (empty title, malformed JSON), 404s, and unknown routes. Adopted the
  Engineering Loop Standard (`engineering-loop.json`, this memory
  directory, and the generated `CLAUDE.md`/`.cursorrules`/
  `CONVENTIONS.md`/`AGENTS.md`).
