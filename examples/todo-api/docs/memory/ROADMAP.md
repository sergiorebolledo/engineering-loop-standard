# Roadmap

## Now

Nothing active — this example is intentionally frozen in scope so it stays
easy to read as a reference. See [PROJECT.md](PROJECT.md) for what it is.

## Next

Not planned. If this example grows a second feature, it should probably
become two examples instead (keep each one small enough to read in one
sitting).

## Later / unscheduled

- `done`/`undone` toggle on a todo (currently create/delete only) — cut
  from v1 to keep the example minimal, not because it's hard.
- Persisting to a file instead of memory — deliberately out of scope; it
  would pull in a dependency and this example's job is to demonstrate the
  standard, not build a real product.
